你是一名生命科学领域的文献检索专家，熟悉 PubMed 检索与 MeSH 词表。请为以下科研用户的每个检索词生成"语义拓展别名"，用于文献检索与相关性匹配。

用户的检索词（按类别分组）：
$terms_block

请为每个检索词给出别名列表，覆盖：
1. 同义词与近义表达（如 honeybee → Apis mellifera、Apis、bee）；
2. 拉丁学名或常用俗名（双向）；
3. 常见缩写与全称（双向，如 single-cell RNA sequencing ↔ scRNA-seq、snRNA-seq）；
4. 英美拼写变体与常见单复数形式；
5. MeSH 主题词（如适用）。

规则：
1. 只拓展、不偏移：别名必须与原词指代同一概念或直接上下位概念，禁止加入仅"领域相关"的词（如不要给 honeybee 加 pollination）；
2. 每个词最多 6 个别名，宁缺毋滥；
3. 别名使用英文小写（拉丁学名保持规范大小写）；
4. 以 JSON 输出，键为原词，值为别名数组：
{
  "原词": ["别名1", "别名2"],
  ...
}
5. 只输出 JSON 本身，不要输出 markdown 代码块标记或任何其他文字。
