你是一名生命科学领域的文献检索专家，熟悉 PubMed 检索策略。一个实验室的全部检索词列在下方（分为"物种词"与"其余检索词"两块，已包含同义词扩展），请把它们按研究主题聚成 3–8 个簇，用于分簇检索文献。

物种词：
$species_block

其余检索词：
$terms_block

规则：
1. 每个检索词必须且只能属于一个簇，不得遗漏任何词；
2. 同一簇内的词应指向同一研究主题（物种词归入研究该物种的主题）；
3. 检索词原样输出，不得改写、翻译或增删；
4. 簇数控制在 3–8 个；主题确实单一时可以少于 3 个；
5. 以 JSON 数组输出，每个簇包含 topic（中文主题名）、species（该簇物种词数组，可为空）、terms（该簇其余检索词数组）：
[
  {"topic": "主题名", "species": ["物种词1"], "terms": ["检索词1", "检索词2"]},
  ...
]
6. 只输出 JSON 本身，不要输出 markdown 代码块标记或任何其他文字。
