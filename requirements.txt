# Literature collection (PubMed / bioRxiv APIs)
requests

# Configuration-driven architecture (config/*.yaml)
PyYAML

# Secrets loaded from .env (never hardcoded)
python-dotenv

# LLM provider (config/model.yaml)
openai

# Testing
pytest

# Daily job scheduling
schedule
